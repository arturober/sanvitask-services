import { TaskCollaboratorGuard } from './task-collaborator.guard';

describe('TaskCollaboratorGuard', () => {
  it('should be defined', () => {
    expect(new TaskCollaboratorGuard()).toBeDefined();
  });
});
